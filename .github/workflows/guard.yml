name: public-guard
on: [push, pull_request]
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 检查公开内容、步骤顺序和本地链接
        run: python3 scripts/check.py
      - name: 检查交互脚本语法
        run: node --check app.js
